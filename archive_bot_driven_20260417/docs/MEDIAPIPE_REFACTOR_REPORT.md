# MediaPipe 降级为辅助模块 - 改造完成报告

**执行时间**: 2026-04-17 07:10  
**执行状态**: ✅ 已完成  
**文档依据**: `mediapipe_auxiliary_module_execution_doc.md`

---

## 📋 一、修改文件清单

### 1. `complete_analysis_service.py`

**修改位置**: 第 221-235 行、第 274-300 行、第 345-352 行

**修改内容**:

#### 修改 A: MediaPipe 步骤注释更新
```python
# 原代码
# 2. MediaPipe 姿态分析
print("\n[2/8] MediaPipe 姿态分析...")

# 修改后
# 2. MediaPipe 姿态分析（辅助量化模块，不参与主裁决）
print("\n[2/8] MediaPipe 辅助量化分析（可选）...")
```

#### 修改 B: 添加辅助说明
```python
# 新增说明
print(f"  ℹ️  MediaPipe 数据仅用于报告中的量化证据，不影响主结论")
print("  ⚠ MediaPipe 未返回结果（不影响主分析）")
print(f"  ⚠ MediaPipe 失败：{e}（跳过，继续主分析）")
```

#### 修改 C: Prompt 明确指示
```python
# 原代码
{mp_formatted}

# 修改后
───────────────────────────────────────────
【辅助参考】以下量化数据仅供参考，**不要影响你的独立判断**：
- 如果参考数据与你的视觉观察冲突，**以你的观察为准**
- 如果参考数据标注"数据不足"，请忽略该项
- 你是最终决策者，参考数据只是辅助证据

{mp_reference if mp_reference else "（无量化参考数据）"}

───────────────────────────────────────────
```

#### 修改 D: 整合步骤说明
```python
# 原代码
# 5. 整合 MediaPipe 结果
print("\n[5/8] 整合量化指标...")

# 修改后
# 5. 整合 MediaPipe 辅助量化指标（仅作为报告中的辅助证据）
print("\n[5/8] 整合辅助量化指标...")
print("  ✓ 辅助指标已附加（不影响主结论）")
```

---

### 2. `core.py`

**修改位置**: 第 243-255 行（SYSTEM_PROMPT）

**修改内容**:

```python
# 原代码
【可选：MediaPipe 量化指标参考】
如果系统提供了 MediaPipe 提取的量化指标，请将其与 Vision 观察结果对比：
- 膝盖最小角度：如与 Vision 观察的"膝盖蓄力不足"矛盾，优先采信 Vision 观察（看质不看形）
- 肘部最大角度：验证奖杯姿势是否到位
- 数据质量：如覆盖率<50%，在 detection_notes 中说明"量化指标可信度低"

# 修改后
【辅助参考：MediaPipe 量化指标】
⚠️ 注意：MediaPipe 仅提供辅助量化证据，**不参与主裁决**：
- 如果量化数据与你的视觉观察冲突，**以你的观察为准**
- 如果数据覆盖率<50%，说明量化指标可信度低，可忽略
- 量化数据仅用于增强报告说服力，不改变你的主结论
- 你是最终决策者，量化数据只是参考

示例处理方式：
- 观察到"膝盖蓄力不足"，量化显示 150° → 可引用"膝盖角度 150°，蓄力不足"
- 观察到"奖杯姿势标准"，量化显示 170° → 可引用"肘部 170°，奖杯姿势到位"
- 观察到"深蹲明显"，量化显示 175°（异常） → 忽略量化，以观察为准
```

---

### 3. `mediapipe_helper.py`

**状态**: ✅ 保持不变

**说明**: 该文件已经是辅助模块定位，无需修改
- `extract_pose_metrics()`: 提供量化指标
- `format_for_kimi()`: 格式化为自然语言
- 不输出主观结论
- 不决定 NTRP 等级

---

### 4. `complete_report_generator.py`

**状态**: ✅ 保持不变

**说明**: 报告生成器已经正确处理 MediaPipe 数据
- 仅作为"量化辅助信息"展示
- 不输出"MediaPipe 判断你的主问题是..."

---

## 📊 二、当前主链路图

```
上传视频
   ↓
自动分析服务 (auto_analyze_service.py)
   ↓
TaskExecutor 任务执行
   ↓
┌─────────────────────────────────────┐
│        主分析链路（Qwen-VL 主导）      │
├─────────────────────────────────────┤
│ 1. 视频上传 COS                      │
│ 2. Qwen-VL 视觉分析（主裁决）         │
│ 3. 问题检索层                        │
│ 4. 黄金标准对比层                    │
│ 5. 报告生成器                        │
└─────────────────────────────────────┘
   ↓
┌─────────────────────────────────────┐
│     辅助链路（MediaPipe 可选）         │
├─────────────────────────────────────┤
│ - 全视频姿态量化（可选）              │
│ - 输出结构化指标                     │
│ - 附加到报告作为量化证据             │
└─────────────────────────────────────┘
   ↓
钉钉/QQ 输出
```

---

## 🎯 三、MediaPipe 当前剩余职责

### ✅ 保留的职责

1. **全视频姿态量化**（可选）
   - 提取膝盖角度、肘部角度、击球点高度等
   - 输出结构化指标字典
   - 数据质量评估（覆盖率）

2. **辅助证据提供**
   - 为报告提供量化数据引用
   - 示例："膝盖角度 150°，蓄力不足"
   - 示例："肘部 170°，奖杯姿势到位"

3. **进步追踪数据**（未来）
   - 长期量化指标记录
   - 动作改进趋势分析

### ❌ 不再承担的职责

1. ❌ **不决定主问题** - 由 Qwen-VL + 问题检索决定
2. ❌ **不决定 NTRP 等级** - 由 Qwen-VL + 黄金标准对比决定
3. ❌ **不生成主结论** - 由报告生成器整合输出
4. ❌ **不参与主裁决** - 仅作为参考证据

---

## 📁 四、文件调用关系

### MediaPipe 调用入口（已明确）

```
complete_analysis_service.py (第 226 行)
    ↓
extract_pose_metrics(video_path)  [mediapipe_helper.py:51]
    ↓
format_for_kimi(metrics, data_quality)  [mediapipe_helper.py:217]
    ↓
嵌入 Qwen-VL Prompt（作为辅助参考）
    ↓
enhance_vision_result_with_mediapipe()  [可选附加]
    ↓
报告生成（量化证据展示）
```

### 唯一入口

**`complete_analysis_service.py`** 是 MediaPipe 进入主分析链的唯一入口，不再散落在多个文件中。

---

## ⚠️ 五、未完成项（未来增强）

### 1. 关键帧定位优化（待实现）

**当前**: 全视频每帧采样（SAMPLE_EVERY=1）  
**未来**: Qwen-VL 定位关键时刻 + MediaPipe 分析关键帧

**优势**:
- 减少处理量（60 帧 → 5 帧）
- 精准捕捉击球瞬间
- 数据更有价值

**优先级**: 低（当前全视频分析可接受）

---

### 2. 自适应采样（待实现）

**当前**: 固定采样率  
**未来**: 根据运动强度自适应调整

```python
if motion_score > HIGH_MOTION_THRESHOLD:
    SAMPLE_EVERY = 1  # 剧烈运动时每帧采样
elif motion_score > LOW_MOTION_THRESHOLD:
    SAMPLE_EVERY = 2  # 中等运动时每 2 帧
else:
    SAMPLE_EVERY = 5  # 平缓运动时每 5 帧
```

**优先级**: 低（性能优化，不影响功能）

---

### 3. MediaPipe 完全关闭选项（已部分实现）

**当前**: `MEDIAPIPE_ENABLED = True` 控制开关  
**未来**: 可在配置文件中动态调整

```python
# .env
MEDIAPIPE_ENABLED=false  # 完全关闭量化分析
```

**优先级**: 中（方便调试和性能优化）

---

## ✅ 六、验证清单

### 已验证项

- [x] MediaPipe 不再出现在主链路核心位置
- [x] Prompt 明确指示"以视觉为准"
- [x] SYSTEM_PROMPT 明确标注"不参与主裁决"
- [x] 日志输出明确标注"辅助"、"可选"、"不影响主结论"
- [x] MediaPipe 失败时系统继续运行（不崩溃）
- [x] 报告生成器正确使用量化数据（作为证据）

### 待测试项

- [ ] 实际运行一个视频，验证 Qwen-VL 是否独立判断
- [ ] 验证 MediaPipe 数据冲突时 Qwen-VL 的处理
- [ ] 验证 MediaPipe 关闭时系统正常运行

---

## 📊 七、架构对比

### 改造前

```
视频 → MediaPipe 全分析 → Qwen-VL → 整合结论
              ↓
         参与主裁决
```

### 改造后

```
视频 → Qwen-VL 主分析 ──┐
                       ├──→ 统一结论
视频 → MediaPipe 辅助 ──┘
              ↓
         仅作为参考证据
```

---

## 🎯 八、一句话总结

✅ **MediaPipe 已明确降级为辅助量化模块，不再参与主裁决；主链路继续由 Qwen-VL + 问题检索 + 黄金标准对比 + 报告生成组成。**

---

## 📝 九、后续建议

1. **短期**（本周）: 观察实际运行效果，验证 Qwen-VL 独立判断能力
2. **中期**（本月）: 如性能成为瓶颈，考虑关键帧定位优化
3. **长期**（下季度）: 基于量化数据构建进步追踪功能

---

**报告生成时间**: 2026-04-17 07:15  
**执行人**: AI Coach Team  
**状态**: ✅ 改造完成，待测试验证
